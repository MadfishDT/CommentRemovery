/**
* This is the foo function
* @param bar This is the bar parameter
* @returns returns a string version of bar
*/
function foo(bar) {
    return bar.toString()
}

/**
* This is the foo function
* @param bar This is the bar parameter
* @returns returns a string version of bar
*/
function foo1(bar) {
    return bar.toString()
}

/*This is the foo function*/
function foo2(bar) {
    return bar.toString()
}

function foo3(bar) {
//this is the foo function
    return bar.toString()
}